global:
  scrape_interval: 15s

scrape_configs:
  # Scrape the collector's Prometheus exporter (app metrics flow OTLP -> collector -> here).
  - job_name: otel-collector
    static_configs:
      - targets: ["otel-collector:8889"]
